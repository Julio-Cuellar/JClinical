# =====================================================
# PATIENT SERVICE - Application Configuration
# =====================================================

# Application Name
spring.application.name=patient-service
server.port=8585

# =====================================================
# PostgreSQL Database Configuration
# =====================================================
spring.datasource.url=jdbc:postgresql://localhost:5432/patient_service_db
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# =====================================================
# JPA/Hibernate Configuration
# =====================================================
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# =====================================================
# JWT Configuration (debe coincidir con Auth Service)
# =====================================================
security.jwt.secret=kYFNJHIRdGmpLHGWL4mRfSGyknywI3bp8PmCdGho
security.jwt.issuer=kYFNJHIRdGmpLHGWL4mRfSGyknywI3bp8PmCdGho
security.jwt.audience=prueba

# =====================================================
# RabbitMQ Configuration
# =====================================================
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Exchange y Queue names para eventos
rabbitmq.exchange.patients=patients.exchange
rabbitmq.queue.patient-created=patient.created.queue
rabbitmq.queue.patient-updated=patient.updated.queue
rabbitmq.queue.patient-deactivated=patient.deactivated.queue
rabbitmq.routing-key.patient-created=patient.created
rabbitmq.routing-key.patient-updated=patient.updated
rabbitmq.routing-key.patient-deactivated=patient.deactivated

# =====================================================
# Eureka Client Configuration
# =====================================================
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true

# =====================================================
# Actuator Configuration
# =====================================================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

